// No content-script logic required right now.
// The popup query uses server-side javascript:gs.getUserID() to scope results
// to the logged-in user without needing to read page globals (which can be
// blocked by strict CSP).
